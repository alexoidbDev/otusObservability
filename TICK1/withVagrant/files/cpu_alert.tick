dbrp "wp_data"."autogen"

stream
  |from()
    .measurement('cpu')
  |alert()
    .crit(lambda: int("usage_idle") < 10)
    //.message('High CPU load. Check your CPU.')
    .log('/tmp/alerts.log')
    .telegram() 